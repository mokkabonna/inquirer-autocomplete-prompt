{
  "name": "inquirer-autocomplete-prompt",
  "description": "Autocomplete prompt for inquirer",
  "version": "0.1.1",
  "dependencies": {
    "chalk": "^1.1.0",
    "figures": "^1.3.5",
    "inquirer": "^0.9.0",
    "lodash": "^3.10.1",
    "util": "^0.10.3"
  },
  "scripts": {
    "lint": "jshint .",
    "checkStyle": "jscs .",
    "pretest": "npm run-script lint && npm run-script checkStyle",
    "test": "istanbul cover ./node_modules/.bin/_mocha test",
    "posttest": "istanbul check-coverage --statements 0 --branches 0 --functions 0 --lines 0 && rm -rf coverage"
  },
  "publishConfig": {
    "registry": "http://registry.npmjs.org"
  },
  "author": "Martin Hansen <martin@martinhansen.no>",
  "license": "ISC",
  "devDependencies": {
    "istanbul": "^0.3.8",
    "jscs": "^1.6.1",
    "jshint": "^2.5.6",
    "mocha": "^2.2.1",
    "promise": "^7.0.4"
  },
  "repository": {
    "type": "git",
    "url": "git@github.com:mokkabonna/inquirer-autocomplete-prompt.git"
  }
}
